# 实施计划（编码任务清单）

项目名：LightTrans（应用显示名：轻译）
文档版本：v1.5（2026-08-28）
执行者：项目开发流程
依据：03-detailed-design.md 与 10-command-line-interface-detailed-design.md。本清单只安排「做什么、什么顺序、怎么验收」，实现细节一律以详细设计为准。任务共 21 项（T1 至 T21）。

## 0. 编码纪律（执行前必读）

1. 严格按详细设计实现；遇到文档未覆盖的决策点，停下向用户说明并补充设计，不得即兴决定。
2. 每完成一个任务立即按其"验收标准"验证通过后，再进入下一任务；不攒多个任务一次性验证。
3. 系统设计第 5 节的待验证假设必须在对应任务中先验证再依赖；A-6 至 A-11 由 T17 验证，不成立时按文档给出的备选处理，并更新设计文档。
4. 注释用中文，代码与文档不使用表情符号。
5. 声称任务完成前，实际构建并运行确认；不得凭"代码写完了"即报告完成。

## 1. 任务清单

### T1 工程骨架

- 内容：创建 Package.swift（依赖 KeyboardShortcuts）、目录结构、空的入口文件，确保 `swift build` 通过。
- 同时验证假设 A-2：确认 KeyboardShortcuts 解析出的版本支持 macOS 14 部署目标；如冲突，按备选处理并更新系统设计文档。
- 验收标准：`swift build` 成功，无警告级以上的问题。

### T2 打包脚本与最小可运行应用

- 内容：Resources/Info.plist、Scripts/build-app.sh；AppDelegate 先只实现状态栏图标与右键菜单（设置项暂为空实现，退出可用）。
- 验收标准：运行脚本产出 LightTrans.app；双击启动后菜单栏出现图标、Dock 无图标（LSUIElement 生效）、右键菜单可退出。

### T3 配置层

- 内容：ConfigStore、KeychainHelper，按详细设计第 2 节实现全部配置项与默认值。
- 验收标准：临时在启动时写入并读回一个测试 Key，确认钥匙串存取正常（验证后删除临时代码）；UserDefaults 各项默认值正确。

### T4 翻译服务

- 内容：TranslationService、SSE 解析、TranslationError 与错误映射，按详细设计第 6、7 节实现。
- 验收标准：
  - 用无效 Key 调一次真实接口，确认走到 invalidKey 分支；
  - 用户提供真实 Key 后，命令行或临时调用验证流式输出逐段到达、[DONE] 正常收尾；
  - 中途取消后连接立即中断（可用日志确认）。
- 说明：本任务需要用户提供一个可用的接口地址与 API Key 做真实验证；拿到之前可先完成编码并用无效 Key 验证错误分支，真流式验证挂起待用户提供。

### T5 浮动面板窗体与快捷键

- 内容：FloatingPanel、togglePanel 定位与显隐逻辑、Option+T 全局快捷键监听。
- 首先验证假设 A-1：面板内放一个临时输入框，真机确认中文输入法（含候选窗）工作正常；不成立则按备选改为激活式面板，并更新设计文档后再继续。
- 验收标准：任意应用前台时按 Option+T 面板呼出且输入框获焦；再按一次隐藏；点击面板外自动隐藏；Esc 隐藏；全屏应用上可呼出。

### T6 面板界面与翻译交互

- 内容：TranslatePanelView、PanelViewModel，按详细设计第 4.2、4.3 节实现完整交互。
- 验收标准：输入文字 Cmd+Return 后译文逐字显示；翻译中可停止；复制按钮工作且有"已复制"反馈；错误场景（断网、无效 Key）显示对应中文文案；隐藏再呼出内容保留。

### T7 历史记录与历史窗口

- 内容：HistoryStore、HistoryWindowView、状态栏菜单"历史记录…"入口、PanelViewModel 翻译结束时写入记录，按详细设计第 10 节实现。
- 首先验证假设 A-4：向 iCloud 云盘目录写入测试文件，确认可直接读写（记录是否出现授权弹窗），并在访达"iCloud 云盘"中确认文件出现；随后验证假设 A-5：对 `.icloud` 占位文件调用下载接口观察行为。任一不成立按系统设计给出的备选处理并回写文档。
- 验收标准：完成、手动停止、失败三种结束状态各产生一条正确记录；记录写入 iCloud 云盘的 `LightTrans/history/history-{设备标识}.jsonl`；历史窗口按时间倒序合并显示、关键字过滤与复制可用；关闭历史开关后不再产生新记录；模拟 iCloud 不可用（临时改判定逻辑或在未登录环境）时自动退化为本机目录。

### T8 设置窗口

- 内容：SettingsView 及 AppDelegate.openSettings，按详细设计第 5 节实现（含历史记录开关与存储位置显示）。
- 验收标准：右键菜单打开设置且窗口获焦可输入（L-2 验证）；各配置项修改后立即生效于下一次翻译；API Key 保存后重启应用仍可用；快捷键重新录制后立即生效。

### T9 开机自启

- 内容：SMAppService 注册/注销，绑定设置开关。
- 验证假设 A-3：将 .app 拷贝至 /Applications 后开启自启，注销重登确认自动启动；失败则按备选（引导手动添加登录项）处理并更新设计文档。
- 验收标准：开关状态与系统"登录项"设置一致；重登验证通过（或已按备选落地）。

### T10 总验收与文档收尾

- 内容：对照需求文档 FR-1 至 FR-11 逐条人工验证，记录结果于本文档第 2 节；更新 README.md（构建、安装、配置说明）。
- 验收标准：FR-1 至 FR-11 全部通过（FR-11 的跨设备部分若暂无第二台电脑，先验证本机写入 iCloud 云盘成功并标注待验）；README 按实际情况更新完毕。

### T11 应用图标与一键安装更新

- 内容：按详细设计第 12 节生成应用图标与 `.icns` 资源；更新 `Info.plist` 和打包脚本；新增一键安装或更新脚本。
- 验收标准：
  - `Resources/AppIcon.png` 为 1024 x 1024 的标准 PNG，包含透明圆角，`Resources/AppIcon.icns` 可被 `iconutil` 识别；
  - 运行 `bash Scripts/build-app.sh` 后，应用包包含图标资源，`Info.plist` 图标配置正确，严格签名校验通过；
  - 首次运行 `bash Scripts/install-app.sh` 可安装并启动应用；再次运行可替换已安装版本并重新启动，配置、钥匙串和历史数据不受影响；
  - 安装过程中若应用正在运行，只退出目标安装路径对应的进程；替换失败时旧版本可恢复。
- 验收结果（2026-07-15）：通过。图标尺寸、透明圆角与全部 ICNS 图层已检查；强制使用 `/Library/Developer/CommandLineTools` 并删除 `.build` 后，重新解析、补丁处理并编译 `KeyboardShortcuts 2.4.0`，release 构建、应用包资源和严格签名校验通过；安装与运行中更新均已执行，更新前后进程号发生变化，安装目录内无临时文件或旧版本备份残留。

### T12 UI-1 浮动翻译面板重构

- 内容：按详细设计第 13.1 节与 UI 方案 v3.0 重构 `FloatingPanel.swift`、`TranslatePanelView.swift`、`PanelViewModel.swift`、`AppDelegate.swift`。
  - 窗体启用 NSVisualEffectView（.popover）材质与 14pt 圆角；
  - 输入区卡片化，增加一键清空（xmark）、字符统计（82 字符）；
  - 维持 Esc 严格隐藏逻辑；
  - 操作栏生成中切换为「停止」按钮；
  - 双路卡片独立展示状态（正在直译/正在转写/完成/失败文案），平分剩余空间；
  - 复制按钮带 1.5 秒绿色 checkmark「已复制」反馈；
  - 底部工具栏通过闭包依赖注入直达设置与历史窗口。
- 验收标准：
  - 在 560 × 600 真实窗口下，所有控件垂直排版与设计预算表吻合，无截断或溢出；
  - 翻译中点击「停止」按钮能立即中断网络请求；
  - 按 Esc 稳定隐藏面板；点击清空仅清除输入；
  - 点击底部齿轮与时钟图标能唤起并获焦设置/历史窗口。
- 验收结果（2026-08-21）：通过。NSVisualEffectView 原生毛玻璃材质与 14pt 圆角生效；输入框 100pt 并与清空/字符数避让无遮挡；生成中主按钮切换为停止；双路独立完成态展示「已完成」；Release 打包及真机交互验证通过。

### T13 UI-2 设置窗口分页与连通测试

- 内容：按详细设计第 13.2 节重构 `SettingsView.swift`，并在 `TranslationService.swift` 中新增 `testConnection` 探测接口。
  - 改造为固定 4 项侧边栏分类导航（接口配置、提示词模板、通用与快捷键、历史记录），窗口固定 520 × 450 pt；
  - 接口配置：模型名保持自由输入，API Key 提供明暗文切换；
  - 连通性测试：发起 `max_tokens: 32` 探测请求（超时 10 秒，支持取消，不写历史），提供毫秒耗时与状态反馈；输出达到上限且正文为空时，明确提示测试输出 Token 不足；
  - 提示词模板：同页展示直译与转写双模板，带 {{text}} 占位符校验徽章。
- 验收标准：
  - 侧边栏分类切换平滑，右侧各页面独立展示无裁切；
  - 输入有效/无效配置点击「测试连接」能准确反馈耗时或错误，且确认不向 HistoryStore 写入记录；
  - 直译与转写双模板均能正常编辑并持久化。
- 验收结果（2026-08-21）：通过。520 × 450 窗口侧边栏 4 项真实分页无裁切；API Key 眼睛图标明暗文切换正常；连通性测试精准反馈耗时/错误且绝不写历史记录；双模板同页展示与占位符徽章正常生效。

### T14 UI-3 历史窗口密度与详情重构

- 内容：按详细设计第 13.3 节重构 `HistoryWindowView.swift`。
  - 列表项摘要最多 2 行，增加语义状态点（绿/橙/红）与无障碍标签，设备名降为次要文本；
  - 搜索栏显示匹配计数，搜索/刷新自动联动选中项（不存在则选首项，无结果置空）；
  - 详情区元数据 2 行网格排版（长文本尾部截断+tooltip），正文分 3 卡片独立排版与复制。
- 验收标准：
  - 680 × 480 尺寸下左右分栏比例协调，长设备名/模型名不溢出；
  - 搜索过滤与空状态展示自然，无旧详情残留；
  - 详情各卡片独立复制与状态展示准确。
- 验收结果（2026-08-21）：通过。680 × 480 尺寸下左侧 2 行摘要与微型语义色点展示清晰，移除蓝色徽章降噪效果明显；搜索实时计数并闭环联动选中/首选/空状态；元数据 2 行网格与 3 张独立正文卡片（带 1.5 秒绿色 checkmark）运行良好。

### T15 UI-4 输入框高度拖拽调节与菜单栏图标精化

- 内容：按详细设计第 13.4 节与 UI 方案 v3.0 重构 `TranslatePanelView.swift` 与 `AppDelegate.swift`。
  - 输入卡片底部配备胶囊拖拽手柄（`36 × 4 pt`），支持 70–240 pt 动态高度拖拽调节，鼠标悬停提供 `.resizeUpDown` 光标，双击弹性复位 100 pt；
  - 面板内双结果卡片弹性平分剩余可用空间，总窗口尺寸严格维持 560 × 600 pt；
  - 菜单栏图标采用 `translate` 模板图标（一级入口表达翻译语义），保留 `character.bubble` 回退，配置 14pt 中等字重，适配各种系统壁纸与深浅色模式。
- 验收标准：
  - 拖拽手柄可平滑调节输入框高度，双击手柄快速复位 100 pt；
  - 输入框变高时，双结果卡平分剩余空间，文字独立平滑滚动，面板无多余溢出或裁切；
  - 状态栏图标显示为清晰的 `translate` 图标，回退 `character.bubble`；深浅色切换正常，点击高亮正常。
- 验收结果（2026-08-21）：通过。输入高度可在 70–240 pt 范围内调节并支持双击复位，双结果区随高度变化保持等分和独立滚动；菜单栏使用 `translate` 模板图标并保留 `character.bubble` 回退。拖拽事件竞争与像素级视觉一致性继续纳入 T16 精修。

### T16 UI 视觉一致性与交互精修

- 内容：按详细设计第 13.5 节与 UI 方案 v4.0、`docs/changes/ui-visual-consistency/spec.md` 完成视觉一致性精修。
  - 翻译面板：操作栏增加圆角容器，结果区改为完整卡片，复制按钮改为紧凑图标，拖拽手柄增加无障碍；
  - 设置窗口：测试连接增加取消入口，API Key 眼睛按钮入尾部，双模板完整卡片；
  - 历史窗口：搜索归左栏，列表改两层，详情完整卡片，三种空状态；
  - 菜单栏：主符号改为 `translate`，保留 `character.bubble` 回退。
- 验收标准：
  - 三个窗口卡片、复制和状态视觉语言一致；
  - 面板 70/100/240 pt 输入高度均无裁切，两个结果区等高且可滚动；
  - 测试连接可取消，长错误不挤压，520 x 450 pt 无裁切；
  - 历史搜索在左侧列表，支持清空、计数、刷新和正确空状态；
  - `git diff --check`、`swift build`、Release 打包和严格签名校验通过。
- 验收结果（2026-08-22）：通过。5/5 单元测试、Debug/Release 构建、应用打包、严格签名和 `git diff --check` 均通过；19 个冻结窗口状态与菜单栏深色、浅色、按下三态完成截图校验，输入高度拖拽、左键打开面板和右键真实菜单完成真机验证；用户确认 T16 验收完成。

### T17 选中文字后打开翻译面板

- 前置条件：
  - `docs/07-selection-translation-feature-design.md` 已于 2026-08-22 确认；
  - T16 已于 2026-08-22 完成用户验收并提交，T17 编码门禁已满足；
  - 编码范围只包含 FR-12 第一阶段，FR-14 单路翻译并复制和 FR-15 原位替换不得顺带实现。
- 内容：按详细设计第 14 节实现 `轻译：打开面板`。
  - 在主应用 `Info.plist` 注册只接收纯文本、无返回类型的 macOS 服务；
  - 新增 `SelectionServiceProvider.swift`，只负责读取请求专用 pasteboard 和生成不可变请求；
  - `AppDelegate` 在面板准备完成后注册服务，收到选区时执行「显示面板」而不是切换显隐；
  - `PanelViewModel` 增加外部文字入口，支持 `5,000` 字符自动执行上限；
  - 翻译中收到新选区时，先停止并保存旧任务，再执行最新请求；
  - 在现有操作栏显示长文本提示，不改变 T16 窗口尺寸和视觉基准；
  - 增加服务请求和外部输入任务生命周期测试。
- 假设验证：
  - 先验证系统设计 A-6：服务注册、Release 安装、退出应用后由服务冷启动；
  - 验证 A-7 至 A-10：不授予「辅助功能」权限，完成已安装目标应用的完整兼容性清单，逐项比对多行、Emoji、组合字符和中英文混排；
  - 验证 A-11：真实流式任务被新选区停止后，只追加一条 `stopped` 历史，最终只执行最新请求；
  - 任一假设不成立时，先按系统设计的备选处理并更新详细设计，不直接扩大到 Accessibility API 或独立 `.service` 包。
- 验收标准：
  - `plutil -lint Resources/Info.plist` 通过，系统服务列表识别 `轻译：打开面板`；
  - 轻译未运行、后台运行、面板已显示三种状态均能接收有效选区；
  - 面板隐藏时按现有规则定位，面板已显示时保留当前位置且不会被服务请求隐藏；
  - 原文的首尾空格、换行、Emoji 和组合字符完整进入输入框；
  - `4,999`、`5,000`、`5,001` 个 Swift `Character` 分别覆盖自动执行边界；
  - 正常选区自动执行直译和转写，单路失败时保留另一路结果；
  - 翻译中连续触发两次时，旧任务写为 `stopped`，最终只执行最新选区，历史数量和内容正确；
  - 仅空白选区不打开面板、不发请求、不写历史；
  - 配置缺失、断网、手动停止、Esc 隐藏和失焦隐藏行为符合现有设计；
  - 第一阶段前后通用剪贴板的 `changeCount` 和内容不变；
  - 未申请「辅助功能」「输入监控」「自动化」或通知权限；
  - 已安装的 TextEdit、备忘录、Safari、Chrome、Cursor、VS Code、Xcode、Terminal、iTerm2、飞书、微信、预览和常用 PDF 阅读器全部记录实际结果，未安装项明确标记为未验证；
  - 新增单元测试、全部现有测试、Debug/Release 构建、Release 打包、严格签名和 `git diff --check` 全部通过；
  - 重新执行现有面板、设置、历史和菜单栏视觉回归，确认 T16 基准未变化；
  - 日志不包含原文、结果、API Key 或 pasteboard 内容。
- 验收结果（2026-08-22）：通过。16 项单元测试、Debug/Release 构建、Release 打包、严格签名、系统服务注册、Safari 冷启动、Chrome/Safari 双路翻译、真实流式任务替换、剪贴板检查、面板位置保持和全量视觉回归均通过；用户确认 Terminal、iTerm2、飞书、微信、PDF Expert 与长文本提示的人工补验全部通过，详见 `08-selection-translation-acceptance.md`。

### T18 GitHub 源码发布准备

- 内容：在不发布预编译二进制、不改写现有 Git 历史的前提下，完成 `v0.1.0` 源码发布准备。
  - 添加 MIT License、变更记录、安全说明、GitHub Actions 和 Dependabot 配置；
  - 重写 README，补全 T12 至 T17 功能、构建方式、隐私、费用和兼容性边界；
  - 统一需求、系统设计、详细设计、实施计划和应用元数据的公开发布口径；
  - API Key 改为原子更新，保存失败时保留旧值并显示错误；
  - 网络层把无有效内容的流式响应判为失败；
  - 为 Keychain、网络响应和历史文件增加隔离单元测试；
  - 清理公开验收样例中的个人设备名和密钥格式误报。
- 验收标准：
  - `plutil -lint`、`git diff --check`、全部单元测试和 Debug 构建通过；
  - Release 应用组装、严格签名校验通过，且不包含 Debug UI 验收字符串；
  - 当前文件和提交历史的敏感信息规则扫描不包含真实凭据；
  - README 不再使用「兼容一切」或「仅本人、不分发」等与源码公开冲突的表述；
  - Git 工作区只包含本任务范围内的文件；不提交、不打标签、不新增远端、不推送。
- 验收结果（2026-08-27）：通过。27/27 单元测试、警告即错误构建、干净依赖重新解析、Debug/Release 构建、应用组装、严格签名、Release Debug 字符串隔离、脚本语法、Plist、YAML、Markdown 链接和敏感信息规则扫描通过；19 个窗口状态与菜单栏深色、浅色、按下三态均取得成功证据。完整脚本曾两次受 Codex 桌面前台竞争中断，失败状态随后通过新增的 `--state` 单状态严格门禁重跑完成。经确认后，源码已发布至 [GitHub](https://github.com/gOODiDEA2002/lighttrans)；首次 GitHub Actions CI 的测试、Debug 构建、Release 应用组装、签名校验和 UI 验收代码隔离全部通过。

### T19 GitHub 未签名预览版发布

- 内容：为没有 Developer ID 证书的 `v0.1.0` 提供可下载的 Apple Silicon 未签名预览版。
  - 更新需求、系统设计和详细设计，明确预览版安全边界与供应链流程；
  - README 增加下载、SHA-256 校验、安装、Gatekeeper 放行和终端备用步骤；
  - 增加可在本地和 GitHub Actions 共用的 Release 附件打包脚本；
  - Tag 推送后由 GitHub Actions 测试、构建、校验、压缩并创建 Pre-release；
  - 附件固定为 `LightTrans-v0.1.0-macos-arm64.zip` 和 `SHA256SUMS`。
- 验收标准：
  - Tag 与 `CFBundleShortVersionString` 一致；
  - 单元测试、应用构建和严格签名校验通过；
  - ZIP 解压后的应用仍通过签名校验，且可执行文件只包含 `arm64`；
  - `shasum -a 256 -c SHA256SUMS` 通过；
  - GitHub Release 标记为 Pre-release，附件名、大小和校验值与 Actions 构建结果一致；
  - README 不把未签名预览版描述为 Apple 已审核或可无提示安装。
- 验收结果（2026-08-27）：通过。发布准备提交和 `v0.1.0` Tag 的常规 CI 均通过；[Release Preview](https://github.com/gOODiDEA2002/lighttrans/actions/runs/33058902845) 完成 27/27 单元测试、应用构建、ad-hoc 签名、ZIP 往返解压、`arm64` 架构和 SHA-256 校验，并创建 [v0.1.0 未签名预览版](https://github.com/gOODiDEA2002/lighttrans/releases/tag/v0.1.0)。从 GitHub 重新下载的 2,760,356 字节 ZIP 再次通过签名、架构和 SHA-256 校验，校验值为 `9dc92078e4e2e3de3e748a5ce9c4b5ca08b715b563eb77aca4d7856caf077ea8`；远端 Tag 指向发布提交 `1c3fd41106ea98d9d2455ba9954af193cafe0275`。

### T20 修复预览版快捷键设置闪退

- 内容：发布 `v0.1.1`，修复从 GitHub Release 下载的应用打开「通用与快捷键」时闪退的问题。
  - 将 SwiftPM 依赖资源包复制到标准的 `Contents/Resources/` 目录；
  - 为固定版本的 `KeyboardShortcuts` 增加最小资源定位兼容补丁；
  - 在发布脚本中增加打包前和解压后的资源位置校验；
  - 更新 README、变更记录和 GitHub Release 说明，并在 `v0.1.0` 页面标明已知问题。
- 验收标准：
  - 单元测试、Debug/Release 构建和严格签名校验通过；
  - ZIP 解压后，资源包仅位于 `Contents/Resources/`，签名、`arm64` 架构和 SHA-256 校验通过；
  - 暂时移除本机 `.build` 回退资源后，从 ZIP 启动应用并打开「通用与快捷键」，进程保持运行且快捷键录制控件正常显示；
  - GitHub Actions 成功创建 `v0.1.1` Pre-release，附件与本地验证结构一致。
- 验收结果（2026-08-27）：通过。27/27 单元测试、干净依赖解析、Release 构建、资源位置、严格签名、`arm64` 架构、ZIP 往返解压和 SHA-256 校验均通过；暂时移除 `.build` 回退资源后，从 ZIP 启动应用并打开「通用与快捷键」，快捷键录制控件正常显示，进程保持运行且没有新增崩溃报告。[master CI](https://github.com/gOODiDEA2002/lighttrans/actions/runs/33063761096)、[Tag CI](https://github.com/gOODiDEA2002/lighttrans/actions/runs/33063768371) 和 [Release Preview](https://github.com/gOODiDEA2002/lighttrans/actions/runs/33063768137) 均成功，[v0.1.1 未签名预览版](https://github.com/gOODiDEA2002/lighttrans/releases/tag/v0.1.1) 已创建。从 GitHub 重新下载的 ZIP 为 2,758,216 字节，SHA-256 为 `f73e37595f184f665c212ef197512f580645e999008effd878f7642014bfcca8`，资源包仅位于 `Contents/Resources/`；`v0.1.0` Release 页面已增加升级警告。

### T21 共享翻译核心与命令行接口

- 前置条件：
  - `docs/09-command-line-interface-feasibility.md` 的方案 A 已于 2026-08-28 确认；
  - `docs/10-command-line-interface-detailed-design.md` v1.0 已于 2026-08-28 确认；
  - 本任务只实现 FR-16，不顺带实施 FR-13 至 FR-15；
  - 不提交、不打 Tag、不推送、不发布，用户确认验收后再单独提交。
- 阶段 1：固定现有行为。
  - 为双路并行、部分失败、手动停止、历史开关结束时读取、外部选区替换增加特征测试；
  - 先运行全部现有测试，确认重构前基线。
- 阶段 2：建立 `LightTransCore`。
  - 拆分 App、Core、CLI 三个 SPM 目标；
  - 迁移配置定义、错误、网络服务和历史结构；
  - App 的 `ConfigStore` 与设置行为保持不变；
  - 完成后立即运行 Core 与现有 App 测试。
- 阶段 3：进程安全历史。
  - 实现 v2 文件、本机 `flock`、`O_APPEND`、短写与 5 秒锁超时；
  - 首次生成 `deviceID` 时增加跨进程锁，防止并发首次启动产生多个历史文件后缀；
  - 新增 legacy/literal/rewrite/both 解码与显示；
  - 使用隔离临时目录完成 50 进程并发、锁失败和 `SIGKILL` 释放验证。
- 阶段 4：共享工作流与 UI 适配。
  - 实现 `TranslationWorkflow`、串行事件、取消优先、结束时历史开关和最终摘要；
  - `PanelViewModel` 改为事件适配器，移除业务调度与历史构造；
  - 重新运行外部选区、面板状态和历史窗口回归。
- 阶段 5：CLI。
  - 实现三种模式、三种格式、位置参数、标准输入、`--help`、`--version`；
  - 实现 stdout/stderr 隔离、退出码、`SIGINT`、`SIGPIPE/EPIPE` 和取消中继；
  - 完成 CLI 合同测试。
- 阶段 6：打包和安装。
  - 把 `lt` 与安装脚本嵌入 App；
  - 扩展本地构建、严格签名、架构、版本、依赖和 ZIP 往返校验；
  - 安装脚本只创建或移除受控符号链接，不修改 Shell 配置。
- 假设验证：
  - A-12、A-13、A-14、A-16、A-17 必须在本任务内验证；
  - A-15 需要在受控真实 iCloud 目录验证，未执行前明确标记为待验；
  - 真实 API 验证使用 App 已保存配置，不读取、打印或保存 API Key；产生费用前保持请求范围最小；
  - 任一关键假设不成立时停止对应后续阶段并更新设计，不关闭历史或改用参数传密钥。
- 验收标准：
  - 三种模式只发起所选路由，默认 `both`，双路并行；
  - 文本、JSON、NDJSON 格式和固定标记符合详细设计；
  - UI 与 CLI 得到相同路由状态、聚合状态、错误和历史语义；
  - `Ctrl+C` 保留部分结果、写一条 `stopped` 历史并返回 `130`；
  - 50 进程并发历史无丢行、交错和坏 JSON，锁失败不无锁退化；
  - App 保存的配置和钥匙串可由 Release CLI 读取；
  - legacy、literal、rewrite、both 历史显示正确；
  - 全部单元测试、警告即错误构建、Debug/Release 构建通过；
  - App 与 CLI 的严格签名、`arm64` 架构、版本、运行时依赖和 ZIP 往返校验通过；
  - 现有面板、设置、历史、菜单栏和 macOS 服务行为无回归；
  - 工作区只包含 T21 与已确认文档范围内的变更。
- 实施结果（2026-08-28）：代码与本机技术验收完成，用户已确认验收并进入提交收尾。60/60 单元与进程测试通过；其中 50 个独立进程并发追加无丢行或坏 JSON，20 个独立进程并发首次启动得到同一 `deviceID`，锁超时、`SIGINT`、`SIGKILL` 和 v1/v2 合并均通过。Release CLI 使用 App 已保存的 UserDefaults 与钥匙串完成 `literal`、`rewrite`、默认 `both` 三种真实请求；对应 iCloud v2 历史分别只包含所请求路由，`SIGINT` 返回 `130` 并写一条 `stopped` 历史，`EPIPE` 返回 `70` 并停止请求。警告即错误构建、App/CLI 严格签名、`arm64`、版本、运行时依赖、ZIP 往返、安装与卸载脚本，以及面板、设置、历史三个代表状态的视觉回归通过。更换 API 供应商后，`literal`、`rewrite`、默认 `both` 三种真实请求再次通过，历史记录的模式与路由输出正确。尚未执行「Release App 与多个 Release CLI 同时写真实 iCloud 目录」和第二台 Mac 的跨设备合并；这两项保留为真机后续验收，不影响临时目录中的进程安全结论。本任务未安装到用户 PATH，未打 Tag、未推送、未发布。

## 2. 总验收记录（T10 时填写）

| 需求 | 结果 | 说明 |
| --- | --- | --- |
| FR-1 | 通过 | T2+T5 真机确认：仅菜单栏图标（UIElement 不进 Dock/Cmd+Tab）、左键呼出面板、右键菜单（历史/设置/退出） |
| FR-2 | 通过 | T5 真机确认：Option+T 呼出/隐藏、Esc 隐藏、全屏应用上可呼出 |
| FR-3 | 通过 | T5+T6 真机确认：浮动面板含输入区、翻译按钮、结果区、复制、状态/错误区 |
| FR-4 | 通过 | T4+T6 真机确认（Hy3 真实接口）：{{text}} 替换、chat/completions 流式追加、翻译中可停止中断 |
| FR-5 | 通过 | T6 真机确认：复制按钮写入剪贴板、"已复制"反馈 |
| FR-6 | 通过 | T8+T9 真机确认：接口地址/API Key/模型名/模板/最大token/快捷键/开机自启均可配置 |
| FR-7 | 通过 | T3+T8 验证：API Key 存钥匙串、其余存 UserDefaults |
| FR-8 | 通过 | T4 错误归类 + T6 真机确认网络错误中文文案；各错误分支映射见详细设计第 7 节 |
| FR-9 | 通过 | T5+T6 真机确认：点面板外部自动隐藏、隐藏不清空、再呼出内容保留 |
| FR-10 | 部分通过 | 完成(done)与真实流式替换产生的 stopped 已真机验证；stopped、failed、历史开关和文件追加读取均有单元测试。真实模型失败后的文件写入仍待专项真机复验 |
| FR-11 | 部分通过 | 本机写入 iCloud 云盘、合并去重/关键字过滤/复制、退化本机目录、.icloud 占位识别均已验；跨设备合并需第二台电脑，标注待验 |
| FR-12 | 通过 | T17 系统服务注册、冷启动、选区无损传递、双路翻译、任务替换、长文本保护、剪贴板不变、兼容性与视觉回归均已验收 |

## 3. 需要用户配合的事项

1. FR-11 完整跨设备验收：在第二台 Mac 上安装应用并验证合并、占位文件下载和同步延迟。
2. 创建 GitHub 仓库后启用 Private vulnerability reporting，并配置仓库说明、主题和默认分支保护。
