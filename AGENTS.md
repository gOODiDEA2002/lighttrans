# 项目规则：mac-translator（轻译 / LightTrans）

本项目为 macOS 菜单栏翻译工具，处于"设计已定、按文档编码"阶段。

## 编码前必读

1. 依次阅读 docs/01-requirements.md、docs/02-system-design.md、docs/03-detailed-design.md，再按 docs/04-implementation-plan.md 的任务顺序（T1 至 T9）执行。
2. 实现细节一律以详细设计（docs/03-detailed-design.md）为准；文档未覆盖的决策点，停下向用户说明并补充设计文档，不得在代码中即兴决定。
3. 系统设计第 4 节"铁律清单"为硬约束，不得违反；第 5 节"待验证假设"须在对应任务中先验证再依赖，结论回写文档。

## 编码规范

- 注释使用中文；代码、注释与文档中不使用表情符号。
- 每个任务完成后按其验收标准实际构建、运行、验证，通过后再进入下一任务。
- 声称"已完成"之前，确认改动已落盘且验证通过。
- 不引入详细设计之外的第三方依赖。
- 每个任务经用户确认验收通过后，将该任务的改动提交为一个 git commit（提交说明格式"T编号: 一句话概括"）；未经确认不得开始下一任务。

## 环境

- 本机 macOS 15.7.4，最低部署目标 macOS 14。
- 工程形态：Swift Package Manager，打包脚本生成 .app（不建 Xcode 工程文件）。
