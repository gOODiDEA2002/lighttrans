# 安全说明

## 支持范围

当前维护 `0.1.x` 版本。项目暂不提供预编译二进制和自动更新服务。

## 报告安全问题

如果 GitHub 仓库已启用 Private vulnerability reporting，请通过仓库的「Security」页面提交私密报告。报告应包含受影响版本、复现步骤、影响范围和可行的缓解方式。

不要在公开 Issue 中提交 API Key、历史记录、选中文字、模型响应、完整本机路径或其他敏感数据。

## 数据边界

- API Key 存储在 macOS 钥匙串。
- 翻译正文会发送到用户配置的模型服务。
- 历史记录是未额外加密的本地或 iCloud 云盘 JSON Lines 文件。
- 应用不包含遥测、账号体系或自有云端服务。

安全问题修复前，建议关闭历史记录、停止使用受影响接口，并撤销可能暴露的 API Key。
