# 变更记录

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [0.2.1] - 2026-08-28

### 修复

- 修复通过 PATH 中的 `lt` 符号链接启动时，`lt --version` 错误显示 `development` 的问题。

### 验证

- 增加通过 PATH 符号链接启动 CLI 的进程级回归测试。
- 61 项单元与进程测试和 CLI 告警即错误构建通过。

## [0.2.0] - 2026-08-28

### 新增

- 增加 `lt` 命令，支持仅直译、仅转写和双路输出，默认执行直译与转写。
- 增加文本、JSON 和 NDJSON 三种输出格式，支持位置参数与标准输入。
- 在应用包中内置 CLI 与安装脚本，可将命令链接安装到 `~/.local/bin/lt`。

### 调整

- App 与 CLI 共用翻译工作流、UserDefaults 配置、macOS 钥匙串和历史记录语义。
- 历史记录改为进程安全追加，支持 App 与多个 CLI 进程并发写入。
- `SIGINT` 和下游管道关闭会取消请求，等待历史处理完成后再退出。

### 验证

- 60 项单元与进程测试通过，包括 50 个进程并发追加、20 个进程并发初始化设备 ID、锁恢复和信号处理。
- App 与 CLI 的 Release 构建、严格签名、`arm64` 架构、版本、运行时依赖和 ZIP 往返校验通过。
- 使用实际模型服务验证仅直译、仅转写和双路输出，三种模式均正确写入 iCloud 历史记录。

## [0.1.1] - 2026-08-27

### 修复

- 修复 GitHub Release 预览版打开「通用与快捷键」时因 `KeyboardShortcuts` 资源包定位错误而闪退的问题。

### 验证

- 发布流程同时校验打包前和 ZIP 解压后的资源位置、应用签名、处理器架构与 SHA-256。

## [0.1.0] - 2026-08-27

### 新增

- macOS 菜单栏翻译面板和全局快捷键。
- 基于 OpenAI Chat Completions 兼容接口的直译与提示词转写。
- 两路流式结果、停止请求、复制反馈和错误提示。
- API、提示词、快捷键、开机启动和历史记录设置。
- macOS 钥匙串中的 API Key 存储。
- 本机与 iCloud 云盘 JSON Lines 历史记录。
- 「轻译：打开面板」macOS 服务和 5,000 字符自动请求保护。
- UI 冻结状态与截图验收工具。

### 修复

- API Key 更新改为原子写入，失败时保留原值并显示错误。
- HTTP 200 但没有有效译文片段的流式响应不再被判定为完成。
- 连通性测试为推理模型预留 32 个输出 Token，并在输出额度不足时显示明确错误，不再误报为返回格式异常。

### 验证

- 单元测试覆盖翻译任务、选中文字、Keychain、网络响应和历史文件。
- Debug、Release、应用组装和严格签名检查纳入发布门槛。
