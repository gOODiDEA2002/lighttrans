# 变更记录

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [0.1.1] - 2026-08-27

### 修复

- 修复 GitHub Release 预览版打开「通用与快捷键」时因 `KeyboardShortcuts` 资源包定位错误而闪退的问题。

### 验证

- 发布流程同时校验打包前和 ZIP 解压后的资源位置、应用签名、处理器架构与 SHA-256。

## [0.1.0] - 2026-08-27

### 新增

- macOS 菜单栏翻译面板和全局快捷键。
- 基于 OpenAI Chat Completions 兼容接口的直译与提示词转写。
- 两路流式结果、停止请求、复制反馈和错误提示。
- API、提示词、快捷键、开机启动和历史记录设置。
- macOS 钥匙串中的 API Key 存储。
- 本机与 iCloud 云盘 JSON Lines 历史记录。
- 「轻译：打开面板」macOS 服务和 5,000 字符自动请求保护。
- UI 冻结状态与截图验收工具。

### 修复

- API Key 更新改为原子写入，失败时保留原值并显示错误。
- HTTP 200 但没有有效译文片段的流式响应不再被判定为完成。
- 连通性测试为推理模型预留 32 个输出 Token，并在输出额度不足时显示明确错误，不再误报为返回格式异常。

### 验证

- 单元测试覆盖翻译任务、选中文字、Keychain、网络响应和历史文件。
- Debug、Release、应用组装和严格签名检查纳入发布门槛。
